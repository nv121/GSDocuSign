
//recieve array, iterate, return single envelopeID then delete, update array from EnvelopeId used

//right now recieve array and return first element
function recieveArr(arr) {
    let currentEnvelopeId=arr[0]
    if (currentEnvelopeId!=null){
       return currentEnvelopeId;
    }
}


